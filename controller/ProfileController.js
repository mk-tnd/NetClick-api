const { sequelize, Profile } = require('../models')


async function createProfile(req, res, next) {
  const { } = req.body
}

function editProfile() { }


module.exports = {
  createProfile,
  editProfile
}